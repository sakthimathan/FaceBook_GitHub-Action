name: CI - Selenium Tests with Extent Report (Ubuntu Runner)

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

    schedule:
      - cron: '0 0 * * 0'  # Runs every Sunday at midnight

jobs:
  selenium-tests:
    runs-on: [self-hosted, Linux]

    steps:
      # Step 1: Checkout the repository
      - name: Checkout code
        uses: actions/checkout@v4

      # Step 2: Set up JDK 21 for Linux
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      # Step 3: Verify Java & Maven
      - name: Verify Environment
        run: |
          java -version
          mvn -version
          pwd

      # -------------------------------------------
      # ✅ SCREEN RECORDING STEPS ADDED HERE
      # -------------------------------------------

      - name: Start virtual display
        run: |
          echo "Starting Xvfb..."
          Xvfb :99 -screen 0 1920x1080x24 &
          export DISPLAY=:99

      - name: Start Screen Recording (FFmpeg)
        run: |
          echo "Starting screen recording..."
          mkdir -p recordings
          ffmpeg -y -f x11grab -video_size 1920x1080 -i :99 -r 30 recordings/test-run.mp4 &
        shell: bash

      # -------------------------------------------

      # Step 4: Build project
      - name: Build with Maven
        run: mvn clean compile

      # Step 5: Run Selenium Tests
      - name: Run Selenium Tests with TestNG
        run: |
          export DISPLAY=:99
          mvn clean test -Dsurefire.suiteXmlFiles=testng.xml

      # -------------------------------------------
      # ✅ STOP SCREEN RECORDING
      # -------------------------------------------
      - name: Stop Screen Recording
        if: always()
        run: |
          echo "Stopping screen recording..."
          pkill ffmpeg || true

      # Upload recorded video
      - name: Upload Test Recording
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: screen-recording
          path: recordings/test-run.mp4
      # -------------------------------------------

      # Step 6: Upload Extent Report as artifact
      - name: Upload Extent Report
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: extent-report
          path: test-output/extent-report.html

      # Step 7: Upload screenshots
      - name: Upload Screenshots
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: screenshots
          path: test-output/screenshots

      # Step 8: Copy screenshots & report to local folder
      - name: Copy Screenshots & Report to Local Folder
        if: always()
        run: |
          timestamp=$(date +"%Y%m%d_%H%M%S")
          baseDest="/mnt/d/GitHubScreenshots/$timestamp"
          
          srcScreenshots="test-output/screenshots"
          srcReport="test-output/extent-report.html"
          
          destScreenshots="$baseDest/screenshots"
          destReports="$baseDest/reports"
          
          mkdir -p "$destScreenshots"
          mkdir -p "$destReports"
          
          if [ -d "$srcScreenshots" ]; then
            cp -r "$srcScreenshots"/* "$destScreenshots" || true
            echo "✅ Screenshots copied to $destScreenshots"
          else
            echo "⚠️ No screenshots found at $srcScreenshots"
          fi
          
          if [ -f "$srcReport" ]; then
            cp "$srcReport" "$destReports/"
            echo "✅ Extent report copied to $destReports"
          else
            echo "⚠️ No Extent Report found at $srcReport"
          fi
          
          echo "🎯 All files saved under: $baseDest"
